module ClientList
  def error_message_for(field, options = {:prepend_text => ""})
    error_message = @client_list.errors[field][0]
    if error_message
      raw "#{options[:prepend_text]} #{error_message}"
    end
  end
end
